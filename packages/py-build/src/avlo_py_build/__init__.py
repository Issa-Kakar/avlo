"""avlo-py-build — the AVLO Python toolchain package (`avlo-build` CLI).

Pack / verify / compress / budgets / stage / publish for the forked-Pyodide
runtime artifacts. Docker build lanes (fork + recipes) remain scripts under
scripts/ until their replatform phases land; everything host-side lives here.
"""
