--- a/pandas/errors/__init__.py	2026-07-19 05:47:18.085075556 -0700
+++ b/pandas/errors/__init__.py	2026-07-19 05:48:04.252082518 -0700
@@ -5,7 +5,6 @@
 from __future__ import annotations
 
 import abc
-import ctypes
 
 from pandas._config.config import OptionError
 
@@ -803,6 +802,9 @@
     """
 
     def __init__(self, message: str) -> None:
+        # avlo: lazy — windows-only path; the wasm build ships no ctypes
+        import ctypes
+
         # attr only exists on Windows, so typing fails on other platforms
         message += f" ({ctypes.WinError()})"  # type: ignore[attr-defined]
         super().__init__(message)
