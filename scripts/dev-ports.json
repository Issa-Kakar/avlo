{
  "_comment": "Base wrangler dev ports per worker. Parallel dev (npm run dev:p) sets PORT_OFFSET=3, so the offset must stay >= max(ports) - min(ports) + 1 = 3 today. Bump to 10 when adding a 4th worker.",
  "main": 8787,
  "images": 8788,
  "unfurl": 8789
}
