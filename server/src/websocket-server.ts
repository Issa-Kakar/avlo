// Stub for WebSocket server - will be implemented in Phase 6B.5
import { Server as HTTPServer } from 'http';
import { ServerEnv } from './config/env.js';

export function setupWebSocketServer(server: HTTPServer, env: ServerEnv) {
  // WebSocket implementation will be added in Phase 6B.5
  console.log('[WebSocket] Server stub - implementation pending Phase 6B.5');
}