diff --git a/packages/editor/src/index.ts b/packages/editor/src/index.ts
index 44da9ee7..7034c619 100644
--- a/packages/editor/src/index.ts
+++ b/packages/editor/src/index.ts
@@ -131,6 +131,11 @@ export {
 	type SnapData,
 	type SnapIndicator,
 } from './lib/editor/managers/SnapManager/SnapManager'
+export { FlatRTree, type FlatRTreeStats } from './lib/editor/managers/SpatialIndexManager/FlatRTree'
+export {
+	ShapeSpatialIndex,
+	SpatialQuery,
+} from './lib/editor/managers/SpatialIndexManager/ShapeSpatialIndex'
 export { SpatialIndexManager } from './lib/editor/managers/SpatialIndexManager/SpatialIndexManager'
 export {
 	TextManager,
diff --git a/packages/editor/src/lib/editor/Editor.ts b/packages/editor/src/lib/editor/Editor.ts
index 1290e665..ba91f6ad 100644
--- a/packages/editor/src/lib/editor/Editor.ts
+++ b/packages/editor/src/lib/editor/Editor.ts
@@ -1122,6 +1122,22 @@ export class Editor extends EventEmitter<TLEventMap> {
 	 */
 	private readonly _spatialIndex: SpatialIndexManager
 
+	/**
+	 * The spatial index.
+	 *
+	 * Reach for {@link SpatialIndexManager.acquireQuery} here rather than
+	 * {@link Editor.getShapeIdsInsideBounds} when the number of shapes the search
+	 * will match is in the same range as the number you are going to `has()` —
+	 * hit tests, brushes, the eraser. A query answers with nothing allocated. A
+	 * `Set` is still right for a caller that probes the whole page for a handful
+	 * of matches, which is what the viewport cull does.
+	 *
+	 * @internal
+	 */
+	getSpatialIndex(): SpatialIndexManager {
+		return this._spatialIndex
+	}
+
 	/**
 	 * A manager for the any asynchronous events and making sure they're
 	 * cleaned up upon disposal.
@@ -5892,185 +5908,197 @@ export class Editor extends EventEmitter<TLEventMap> {
 
 		// Use larger margin for spatial search to account for edge distance checks
 		const searchMargin = Math.max(innerMargin, outerMargin, this.getHitTestMargin())
-		const candidateIds = this._spatialIndex.getShapeIdsAtPoint(point, searchMargin)
-
-		const shapesToCheck = opts.renderingOnly
-			? this.getCurrentPageRenderingShapesSorted()
-			: this.getCurrentPageShapesSorted()
+		// Taken from the pool and released below. It cannot be a single query kept
+		// on the editor: the loop calls `opts.filter` and `getShapeGeometry`, both
+		// of which run user code, and a nested `getShapeAtPoint` from either would
+		// then be reading and writing the same membership.
+		const candidateIds = this._spatialIndex.searchAtPoint(
+			this._spatialIndex.acquireQuery(),
+			point,
+			searchMargin
+		)
 
-		for (let i = shapesToCheck.length - 1; i >= 0; i--) {
-			const shape = shapesToCheck[i]
-			// Frame-like shapes have labels positioned above the shape (outside bounds), so always include them
-			if (!candidateIds.has(shape.id) && !this.isShapeFrameLike(shape)) continue
-			if (
-				(shape.isLocked && !hitLocked) ||
-				this.isShapeHidden(shape) ||
-				this.isShapeOfType(shape, 'group')
-			) {
-				continue
-			}
-			const pageMask = this.getShapeMask(shape)
-			if (pageMask && !pointInPolygon(point, pageMask)) continue
-			if (filter && !filter(shape)) continue
+		try {
+			const shapesToCheck = opts.renderingOnly
+				? this.getCurrentPageRenderingShapesSorted()
+				: this.getCurrentPageShapesSorted()
+
+			for (let i = shapesToCheck.length - 1; i >= 0; i--) {
+				const shape = shapesToCheck[i]
+				// Frame-like shapes have labels positioned above the shape (outside bounds), so always include them
+				if (!candidateIds.has(shape.id) && !this.isShapeFrameLike(shape)) continue
+				if (
+					(shape.isLocked && !hitLocked) ||
+					this.isShapeHidden(shape) ||
+					this.isShapeOfType(shape, 'group')
+				) {
+					continue
+				}
+				const pageMask = this.getShapeMask(shape)
+				if (pageMask && !pointInPolygon(point, pageMask)) continue
+				if (filter && !filter(shape)) continue
 
-			const geometry = this.getShapeGeometry(shape)
-			const isGroup = geometry instanceof Group2d
+				const geometry = this.getShapeGeometry(shape)
+				const isGroup = geometry instanceof Group2d
 
-			const pointInShapeSpace = this.getPointInShapeSpace(shape, point)
+				const pointInShapeSpace = this.getPointInShapeSpace(shape, point)
 
-			// Check labels first. Only group geometries can carry a label child; a frame-like
-			// shape util may still return a plain geometry.
-			const shapeUtil = this.getShapeUtil(shape)
-			const isShapeFrameLike = this.isShapeFrameLike(shape)
-			if (
-				isGroup &&
-				(isShapeFrameLike ||
-					((this.isShapeOfType(shape, 'note') ||
-						this.isShapeOfType(shape, 'arrow') ||
-						(this.isShapeOfType(shape, 'geo') && shape.props.fill === 'none')) &&
-						shapeUtil.getText(shape)?.trim()))
-			) {
-				for (const childGeometry of geometry.children) {
-					if (childGeometry.isLabel && childGeometry.isPointInBounds(pointInShapeSpace)) {
-						return shape
+				// Check labels first. Only group geometries can carry a label child; a frame-like
+				// shape util may still return a plain geometry.
+				const shapeUtil = this.getShapeUtil(shape)
+				const isShapeFrameLike = this.isShapeFrameLike(shape)
+				if (
+					isGroup &&
+					(isShapeFrameLike ||
+						((this.isShapeOfType(shape, 'note') ||
+							this.isShapeOfType(shape, 'arrow') ||
+							(this.isShapeOfType(shape, 'geo') && shape.props.fill === 'none')) &&
+							shapeUtil.getText(shape)?.trim()))
+				) {
+					for (const childGeometry of geometry.children) {
+						if (childGeometry.isLabel && childGeometry.isPointInBounds(pointInShapeSpace)) {
+							return shape
+						}
 					}
 				}
-			}
 
-			if (isShapeFrameLike) {
-				// On the rare case that we've hit a frame-like shape (not its label), test again hitInside to be forced true;
-				// this prevents clicks from passing through the body of a frame to shapes behind it.
+				if (isShapeFrameLike) {
+					// On the rare case that we've hit a frame-like shape (not its label), test again hitInside to be forced true;
+					// this prevents clicks from passing through the body of a frame to shapes behind it.
+
+					// If the hit is within the frame's outer margin, then select the frame
+					const distance = geometry.distanceToPoint(pointInShapeSpace, hitFrameInside)
+					if (
+						hitFrameInside
+							? (distance > 0 && distance <= outerMargin) ||
+								(distance <= 0 && distance > -innerMargin)
+							: distance > 0 && distance <= outerMargin
+					) {
+						return inMarginClosestToEdgeHit || shape
+					}
 
-				// If the hit is within the frame's outer margin, then select the frame
-				const distance = geometry.distanceToPoint(pointInShapeSpace, hitFrameInside)
-				if (
-					hitFrameInside
-						? (distance > 0 && distance <= outerMargin) ||
-							(distance <= 0 && distance > -innerMargin)
-						: distance > 0 && distance <= outerMargin
-				) {
-					return inMarginClosestToEdgeHit || shape
+					if (geometry.hitTestPoint(pointInShapeSpace, 0, true)) {
+						// Once we've hit a frame, we want to end the search. If we have hit a shape
+						// already, then this would either be above the frame or a child of the frame,
+						// so we want to return that. Otherwise, the point is in the empty space of the
+						// frame. If `hitFrameInside` is true (e.g. used drawing an arrow into the
+						// frame) we the frame itself; other wise, (e.g. when hovering or pointing)
+						// we would want to return null.
+						return (
+							inMarginClosestToEdgeHit ||
+							inHollowSmallestAreaHit ||
+							(hitFrameInside ? shape : undefined)
+						)
+					}
+					continue
 				}
 
-				if (geometry.hitTestPoint(pointInShapeSpace, 0, true)) {
-					// Once we've hit a frame, we want to end the search. If we have hit a shape
-					// already, then this would either be above the frame or a child of the frame,
-					// so we want to return that. Otherwise, the point is in the empty space of the
-					// frame. If `hitFrameInside` is true (e.g. used drawing an arrow into the
-					// frame) we the frame itself; other wise, (e.g. when hovering or pointing)
-					// we would want to return null.
-					return (
-						inMarginClosestToEdgeHit ||
-						inHollowSmallestAreaHit ||
-						(hitFrameInside ? shape : undefined)
-					)
-				}
-				continue
-			}
-
-			let distance: number
+				let distance: number
 
-			if (isGroup) {
-				let minDistance = Infinity
-				for (const childGeometry of geometry.children) {
-					if (childGeometry.isLabel && !hitLabels) continue
+				if (isGroup) {
+					let minDistance = Infinity
+					for (const childGeometry of geometry.children) {
+						if (childGeometry.isLabel && !hitLabels) continue
 
-					// hit test the all of the child geometries that aren't labels
-					const tDistance = childGeometry.distanceToPoint(pointInShapeSpace, hitInside)
-					if (tDistance < minDistance) {
-						minDistance = tDistance
+						// hit test the all of the child geometries that aren't labels
+						const tDistance = childGeometry.distanceToPoint(pointInShapeSpace, hitInside)
+						if (tDistance < minDistance) {
+							minDistance = tDistance
+						}
 					}
-				}
 
-				distance = minDistance
-			} else {
-				// If the margin is zero and the geometry has a very small width or height,
-				// then check the actual distance. This is to prevent a bug where straight
-				// lines would never pass the broad phase (point-in-bounds) check.
-				if (outerMargin === 0 && (geometry.bounds.w < 1 || geometry.bounds.h < 1)) {
-					distance = geometry.distanceToPoint(pointInShapeSpace, hitInside)
+					distance = minDistance
 				} else {
-					// Broad phase
-					if (geometry.bounds.containsPoint(pointInShapeSpace, outerMargin)) {
-						// Narrow phase (actual distance)
+					// If the margin is zero and the geometry has a very small width or height,
+					// then check the actual distance. This is to prevent a bug where straight
+					// lines would never pass the broad phase (point-in-bounds) check.
+					if (outerMargin === 0 && (geometry.bounds.w < 1 || geometry.bounds.h < 1)) {
 						distance = geometry.distanceToPoint(pointInShapeSpace, hitInside)
 					} else {
-						// Failed the broad phase, geddafugaotta'ere!
-						distance = Infinity
+						// Broad phase
+						if (geometry.bounds.containsPoint(pointInShapeSpace, outerMargin)) {
+							// Narrow phase (actual distance)
+							distance = geometry.distanceToPoint(pointInShapeSpace, hitInside)
+						} else {
+							// Failed the broad phase, geddafugaotta'ere!
+							distance = Infinity
+						}
 					}
 				}
-			}
-
-			if (geometry.isClosed) {
-				// For closed shapes, the distance will be positive if outside of
-				// the shape or negative if inside of the shape. If the distance
-				// is greater than the margin, then it's a miss. Otherwise...
 
-				// Are we close to the shape's edge?
-				if (distance <= outerMargin || (hitInside && distance <= 0 && distance > -innerMargin)) {
-					if (geometry.isFilled || (isGroup && geometry.children[0].isFilled)) {
-						// If the geometry rejects this hit (e.g. transparent image pixel),
-						// skip this shape and check shapes behind it.
-						if (geometry.ignoreHit(pointInShapeSpace)) {
-							continue
-						}
-						// If the shape is filled, then it's a hit. Remember, we're
-						// starting from the TOP-MOST shape in z-index order, so any
-						// other hits would be occluded by the shape.
-						return inMarginClosestToEdgeHit || shape
-					} else {
-						// If the shape is bigger than the viewport, then skip it.
-						if (this.getShapePageBounds(shape)!.contains(viewportPageBounds)) continue
-
-						// If we're close to the edge of the shape, and if it's the closest edge among
-						// all the edges that we've gotten close to so far, then we will want to hit the
-						// shape unless we hit something else or closer in later iterations.
-						if (
-							hitInside
-								? // On hitInside, the distance will be negative for hits inside
-									// If the distance is positive, check against the outer margin
-									(distance > 0 && distance <= outerMargin) ||
-									// If the distance is negative, check against the inner margin
-									(distance <= 0 && distance > -innerMargin)
-								: // If hitInside is false, then sadly _we do not know_ whether the
-									// point is inside or outside of the shape, so we check against
-									// the max of the two margins
-									Math.abs(distance) <= Math.max(innerMargin, outerMargin)
-						) {
-							if (Math.abs(distance) < inMarginClosestToEdgeDistance) {
-								inMarginClosestToEdgeDistance = Math.abs(distance)
-								inMarginClosestToEdgeHit = shape
+				if (geometry.isClosed) {
+					// For closed shapes, the distance will be positive if outside of
+					// the shape or negative if inside of the shape. If the distance
+					// is greater than the margin, then it's a miss. Otherwise...
+
+					// Are we close to the shape's edge?
+					if (distance <= outerMargin || (hitInside && distance <= 0 && distance > -innerMargin)) {
+						if (geometry.isFilled || (isGroup && geometry.children[0].isFilled)) {
+							// If the geometry rejects this hit (e.g. transparent image pixel),
+							// skip this shape and check shapes behind it.
+							if (geometry.ignoreHit(pointInShapeSpace)) {
+								continue
 							}
-						} else if (!inMarginClosestToEdgeHit) {
-							// If we're not within margin distance to any edge, and if the
-							// shape is hollow, then we want to hit the shape with the
-							// smallest area. (There's a bug here with self-intersecting
-							// shapes, like a closed drawing of an "8", but that's a bigger
-							// problem to solve.)
-							const { area } = geometry
-							if (area < inHollowSmallestArea) {
-								inHollowSmallestArea = area
-								inHollowSmallestAreaHit = shape
+							// If the shape is filled, then it's a hit. Remember, we're
+							// starting from the TOP-MOST shape in z-index order, so any
+							// other hits would be occluded by the shape.
+							return inMarginClosestToEdgeHit || shape
+						} else {
+							// If the shape is bigger than the viewport, then skip it.
+							if (this.getShapePageBounds(shape)!.contains(viewportPageBounds)) continue
+
+							// If we're close to the edge of the shape, and if it's the closest edge among
+							// all the edges that we've gotten close to so far, then we will want to hit the
+							// shape unless we hit something else or closer in later iterations.
+							if (
+								hitInside
+									? // On hitInside, the distance will be negative for hits inside
+										// If the distance is positive, check against the outer margin
+										(distance > 0 && distance <= outerMargin) ||
+										// If the distance is negative, check against the inner margin
+										(distance <= 0 && distance > -innerMargin)
+									: // If hitInside is false, then sadly _we do not know_ whether the
+										// point is inside or outside of the shape, so we check against
+										// the max of the two margins
+										Math.abs(distance) <= Math.max(innerMargin, outerMargin)
+							) {
+								if (Math.abs(distance) < inMarginClosestToEdgeDistance) {
+									inMarginClosestToEdgeDistance = Math.abs(distance)
+									inMarginClosestToEdgeHit = shape
+								}
+							} else if (!inMarginClosestToEdgeHit) {
+								// If we're not within margin distance to any edge, and if the
+								// shape is hollow, then we want to hit the shape with the
+								// smallest area. (There's a bug here with self-intersecting
+								// shapes, like a closed drawing of an "8", but that's a bigger
+								// problem to solve.)
+								const { area } = geometry
+								if (area < inHollowSmallestArea) {
+									inHollowSmallestArea = area
+									inHollowSmallestAreaHit = shape
+								}
 							}
 						}
 					}
-				}
-			} else {
-				// For open shapes (e.g. lines or draw shapes) always use the margin.
-				// If the distance is less than the margin, return the shape as the hit.
-				// Use the editor's configurable hit test margin.
-				if (distance < this.getHitTestMargin()) {
-					return shape
+				} else {
+					// For open shapes (e.g. lines or draw shapes) always use the margin.
+					// If the distance is less than the margin, return the shape as the hit.
+					// Use the editor's configurable hit test margin.
+					if (distance < this.getHitTestMargin()) {
+						return shape
+					}
 				}
 			}
-		}
 
-		// If we haven't hit any filled shapes or frames, then return either
-		// the shape who we hit within the margin (and of those, the one that
-		// had the shortest distance between the point and the shape edge),
-		// or else the hollow shape with the smallest area—or if we didn't hit
-		// any margins or any hollow shapes, then null.
-		return inMarginClosestToEdgeHit || inHollowSmallestAreaHit || undefined
+			// If we haven't hit any filled shapes or frames, then return either
+			// the shape who we hit within the margin (and of those, the one that
+			// had the shortest distance between the point and the shape edge),
+			// or else the hollow shape with the smallest area—or if we didn't hit
+			// any margins or any hollow shapes, then null.
+			return inMarginClosestToEdgeHit || inHollowSmallestAreaHit || undefined
+		} finally {
+			candidateIds.release()
+		}
 	}
 
 	/**
@@ -6094,20 +6122,28 @@ export class Editor extends EventEmitter<TLEventMap> {
 		opts = {} as { margin?: number; hitInside?: boolean }
 	): TLShape[] {
 		const margin = opts.margin ?? 0
-		const candidateIds = this._spatialIndex.getShapeIdsAtPoint(point, margin)
+		const candidateIds = this._spatialIndex.searchAtPoint(
+			this._spatialIndex.acquireQuery(),
+			point,
+			margin
+		)
 
-		// Get all page shapes in z-index order and filter to candidates that pass isPointInShape.
-		// Frame-like shapes are always checked because their labels can be outside their bounds.
-		// Iterate backwards so the result is pre-sorted in reverse z-index order (top-most first).
-		const sorted = this.getCurrentPageShapesSorted()
-		const result: TLShape[] = []
-		for (let i = sorted.length - 1; i >= 0; i--) {
-			const shape = sorted[i]
-			if (this.isShapeHidden(shape)) continue
-			if (!candidateIds.has(shape.id) && !this.isShapeFrameLike(shape)) continue
-			if (this.isPointInShape(shape, point, opts)) result.push(shape)
+		try {
+			// Get all page shapes in z-index order and filter to candidates that pass isPointInShape.
+			// Frame-like shapes are always checked because their labels can be outside their bounds.
+			// Iterate backwards so the result is pre-sorted in reverse z-index order (top-most first).
+			const sorted = this.getCurrentPageShapesSorted()
+			const result: TLShape[] = []
+			for (let i = sorted.length - 1; i >= 0; i--) {
+				const shape = sorted[i]
+				if (this.isShapeHidden(shape)) continue
+				if (!candidateIds.has(shape.id) && !this.isShapeFrameLike(shape)) continue
+				if (this.isPointInShape(shape, point, opts)) result.push(shape)
+			}
+			return result
+		} finally {
+			candidateIds.release()
 		}
-		return result
 	}
 
 	/**
diff --git a/packages/editor/src/lib/editor/derivations/notVisibleShapes.ts b/packages/editor/src/lib/editor/derivations/notVisibleShapes.ts
index cde6a3e3..16c3184e 100644
--- a/packages/editor/src/lib/editor/derivations/notVisibleShapes.ts
+++ b/packages/editor/src/lib/editor/derivations/notVisibleShapes.ts
@@ -12,10 +12,15 @@ import { ShapeUtil } from '../shapes/ShapeUtil'
 export function notVisibleShapes(editor: Editor) {
 	const emptySet = new Set<TLShapeId>()
 	const defaultCanCull = ShapeUtil.prototype.canCull
-
 	return computed<Set<TLShapeId>>('notVisibleShapes', function (prevValue) {
 		const allShapeIds = editor.getCurrentPageShapeIds()
 		const viewportPageBounds = editor.getViewportPageBounds()
+		// A Set is the right shape here, and this is the one place in the SDK
+		// where it is: the loop below probes every shape on the page for a result
+		// that is usually a few hundred, so a small Set is a smaller table to hit
+		// repeatedly than a page-sized id-to-slot map. The index's
+		// allocation-free query path is for the other consumers, whose probes are
+		// proportional to what the search matched.
 		const visibleIds = editor.getShapeIdsInsideBounds(viewportPageBounds)
 
 		// Fast path: if all shapes are visible, return empty set
diff --git a/packages/editor/src/lib/editor/managers/SpatialIndexManager/RBushIndex.ts b/packages/editor/src/lib/editor/managers/SpatialIndexManager/RBushIndex.ts
index a938cfd7..a6e7a7fe 100644
--- a/packages/editor/src/lib/editor/managers/SpatialIndexManager/RBushIndex.ts
+++ b/packages/editor/src/lib/editor/managers/SpatialIndexManager/RBushIndex.ts
@@ -1,6 +1,6 @@
 import type { TLShapeId } from '@tldraw/tlschema'
 import RBush from 'rbush'
-import { Box } from '../../../primitives/Box'
+import type { Box } from '../../../primitives/Box'
 
 /**
  * Element stored in the R-tree spatial index.
diff --git a/packages/editor/src/lib/editor/managers/SpatialIndexManager/SpatialIndexManager.ts b/packages/editor/src/lib/editor/managers/SpatialIndexManager/SpatialIndexManager.ts
index 2d1dcde5..5c7ab3ee 100644
--- a/packages/editor/src/lib/editor/managers/SpatialIndexManager/SpatialIndexManager.ts
+++ b/packages/editor/src/lib/editor/managers/SpatialIndexManager/SpatialIndexManager.ts
@@ -9,14 +9,15 @@ import type { RecordsDiff } from '@tldraw/store'
 import { TLPageId, TLShape, TLShapeId, isBinding, isPageId, isShape } from '@tldraw/tlschema'
 import type { TLRecord } from '@tldraw/tlschema'
 import { objectMapValues } from '@tldraw/utils'
-import { Box } from '../../../primitives/Box'
+import type { Box } from '../../../primitives/Box'
 import type { Editor } from '../../Editor'
-import { RBushIndex, type SpatialElement } from './RBushIndex'
+import { ShapeSpatialIndex, type SpatialQuery } from './ShapeSpatialIndex'
 
 /**
  * Manages spatial indexing for efficient shape location queries.
  *
- * Uses an R-tree (via RBush) to enable O(log n) spatial queries instead of O(n) iteration.
+ * Uses an R-tree ({@link ShapeSpatialIndex}) to enable O(log n) spatial queries instead of O(n)
+ * iteration.
  * Derived-bounds changes (arrows following bound shapes, groups resizing with children) are
  * tracked structurally: each record diff is expanded into a dirty set covering the changed
  * shapes' descendants, ancestors, and binding partners, and only those shapes' bounds are
@@ -38,17 +39,17 @@ import { RBushIndex, type SpatialElement } from './RBushIndex'
  * @internal
  */
 export class SpatialIndexManager {
-	private rbush: RBushIndex
+	private index: ShapeSpatialIndex
 	private spatialIndexComputed: Computed<number>
 	private lastPageId: TLPageId | null = null
 
-	// Bumps only when the rbush content may have changed. Consumers subscribe
+	// Bumps only when the index content may have changed. Consumers subscribe
 	// via the computed; a stable epoch lets prop-only diffs skip downstream
 	// invalidations.
 	private _boundsEpoch = 0
 
 	constructor(public readonly editor: Editor) {
-		this.rbush = new RBushIndex()
+		this.index = new ShapeSpatialIndex()
 		this.spatialIndexComputed = this.createSpatialIndexComputed()
 	}
 
@@ -95,35 +96,36 @@ export class SpatialIndexManager {
 	}
 
 	private buildFromScratch(): void {
-		this.rbush.clear()
 		this.lastPageId = this.editor.getCurrentPageId()
 
-		const elements: SpatialElement[] = []
-		for (const shape of this.editor.getCurrentPageShapes()) {
-			const bounds = this.editor.getShapePageBounds(shape.id)
-			if (bounds && bounds.isValid()) {
-				elements.push({
-					minX: bounds.minX,
-					minY: bounds.minY,
-					maxX: bounds.maxX,
-					maxY: bounds.maxY,
-					id: shape.id,
-				})
+		// Staged straight into the index's own typed buffers: a page rebuild
+		// allocates nothing per shape, and the packed bulk load reads them in
+		// place. Reading the id set rather than `getCurrentPageShapes()` also
+		// skips materializing a page-sized array of shape records that only
+		// their ids are wanted from — and rebuilds are not rare, since a
+		// hundred store transactions without a read of this computed overflow
+		// the diff buffer and force one.
+		const shapeIds = this.editor.getCurrentPageShapeIds()
+		this.index.beginLoad(shapeIds.size)
+		for (const id of shapeIds) {
+			const bounds = this.editor.getShapePageBounds(id)
+			if (bounds) {
+				this.index.stage(id, bounds.minX, bounds.minY, bounds.maxX, bounds.maxY)
 			}
 		}
-
-		// Bulk load for efficiency
-		this.rbush.bulkLoad(elements)
+		this.index.commitLoad()
 	}
 
 	private processIncrementalUpdate(
 		shapeDiff: RecordsDiff<TLRecord>[],
 		bindingDiff: RecordsDiff<TLRecord>[]
 	): boolean {
-		// Rbush mutations are collected and applied as one batch at the end so
-		// large updates can use a bulk rebuild instead of per-item tree ops.
+		// Removals are collected because step 4 can cancel one (a shape removed
+		// and re-added inside the same diff batch); upserts are applied as they
+		// are decided, since the index resolves a move in place and has nothing
+		// to gain from seeing them together.
 		const pendingRemoves = new Set<TLShapeId>()
-		const pendingUpserts: SpatialElement[] = []
+		let changed = false
 
 		// Shapes whose page transform may have moved (their record was added or
 		// updated). Their descendants' transforms move with them.
@@ -150,7 +152,7 @@ export class SpatialIndexManager {
 
 			for (const shape of objectMapValues(changes.removed) as TLShape[]) {
 				if (!isShape(shape)) continue
-				if (this.rbush.has(shape.id)) {
+				if (this.index.has(shape.id)) {
 					pendingRemoves.add(shape.id)
 				}
 				// The old parent's derived bounds (groups) may shrink. Bindings
@@ -228,14 +230,9 @@ export class SpatialIndexManager {
 			}
 		}
 
-		// Step 4: recheck only the dirty shapes' bounds. The batch stays empty
-		// on prop-only updates with unchanged bounds, so they don't bump the
-		// epoch.
+		// Step 4: recheck only the dirty shapes' bounds. Nothing is applied on
+		// prop-only updates with unchanged bounds, so they don't bump the epoch.
 		for (const id of dirty) {
-			// A shape removed in an earlier diff entry may have been re-added in
-			// a later one; its pending removal means the indexed bounds no
-			// longer count.
-			const indexedElement = pendingRemoves.has(id) ? undefined : this.rbush.getElement(id)
 			const record = this.editor.store.unsafeGetWithoutCapture(id)
 
 			if (
@@ -243,42 +240,41 @@ export class SpatialIndexManager {
 				!isShape(record) ||
 				this.editor.getAncestorPageId(record) !== this.lastPageId
 			) {
-				if (this.rbush.has(id)) {
+				if (this.index.has(id)) {
 					pendingRemoves.add(id)
 				}
 				continue
 			}
 
 			const bounds = this.editor.getShapePageBounds(id)
-			if (bounds && bounds.isValid()) {
-				if (!this.areBoundsEqualToSpatialElement(bounds, indexedElement)) {
+			if (bounds) {
+				// A shape removed in an earlier diff entry may have been re-added
+				// in a later one; its pending removal means the indexed bounds no
+				// longer count, so it has to be re-upserted even if they match.
+				const unchanged =
+					!pendingRemoves.has(id) &&
+					this.index.matchesBounds(id, bounds.minX, bounds.minY, bounds.maxX, bounds.maxY)
+				if (!unchanged) {
 					pendingRemoves.delete(id)
-					pendingUpserts.push({
-						minX: bounds.minX,
-						minY: bounds.minY,
-						maxX: bounds.maxX,
-						maxY: bounds.maxY,
-						id,
-					})
+					// `upsert` drops a shape whose bounds are not indexable, which
+					// is the `Box.isValid()` check that used to live here, and
+					// reports whether the index actually changed — a shape that was
+					// never indexed and still has unusable bounds must not tick the
+					// epoch.
+					if (this.index.upsert(id, bounds.minX, bounds.minY, bounds.maxX, bounds.maxY)) {
+						changed = true
+					}
 				}
-			} else if (this.rbush.has(id)) {
+			} else if (this.index.has(id)) {
 				pendingRemoves.add(id)
 			}
 		}
 
-		if (pendingRemoves.size === 0 && pendingUpserts.length === 0) return false
-
-		this.rbush.applyBatch(pendingRemoves, pendingUpserts)
-		return true
-	}
+		for (const id of pendingRemoves) {
+			if (this.index.remove(id)) changed = true
+		}
 
-	private areBoundsEqualToSpatialElement(
-		a: Box | undefined,
-		b: SpatialElement | undefined
-	): boolean {
-		if (!a && !b) return true
-		if (!a || !b) return false
-		return a.minX === b.minX && a.minY === b.minY && a.maxX === b.maxX && a.maxY === b.maxY
+		return changed
 	}
 
 	/**
@@ -298,7 +294,7 @@ export class SpatialIndexManager {
 	 */
 	getShapeIdsInsideBounds(bounds: Box): Set<TLShapeId> {
 		this.spatialIndexComputed.get()
-		return this.rbush.search(bounds)
+		return this.index.searchToSet(bounds.minX, bounds.minY, bounds.maxX, bounds.maxY, false)
 	}
 
 	/**
@@ -319,7 +315,65 @@ export class SpatialIndexManager {
 	 */
 	getShapeIdsAtPoint(point: { x: number; y: number }, margin = 0): Set<TLShapeId> {
 		this.spatialIndexComputed.get()
-		return this.rbush.search(new Box(point.x - margin, point.y - margin, margin * 2, margin * 2))
+		return this.index.searchToSet(
+			point.x - margin,
+			point.y - margin,
+			point.x + margin,
+			point.y + margin,
+			true
+		)
+	}
+
+	/**
+	 * Take a reusable query object for the allocation-free search path.
+	 *
+	 * A `Set<TLShapeId>` is the wrong shape for every consumer inside this repo:
+	 * they ask a search result for its `size` and then for `has()` while walking
+	 * the page's sorted shapes, and never keep it. Building the Set costs one
+	 * entry per hit — a few thousand of them on a viewport cull, every frame the
+	 * camera moves. A query answers the same two questions off a generation stamp
+	 * with nothing allocated.
+	 *
+	 * Acquire once and keep it (a long-lived derivation), or acquire and
+	 * {@link SpatialQuery.release} around a single use — the pool makes the
+	 * second free. Each query owns its own stamps, so a search that happens while
+	 * another query's results are still being read cannot corrupt them.
+	 *
+	 * @internal
+	 */
+	acquireQuery(): SpatialQuery {
+		return this.index.acquireQuery()
+	}
+
+	/**
+	 * Run `query` over a rect. Use for viewport-scale rects: culls, marquees,
+	 * brushes.
+	 *
+	 * @internal
+	 */
+	searchBounds(query: SpatialQuery, bounds: Box): SpatialQuery {
+		this.spatialIndexComputed.get()
+		return query.searchBounds(bounds.minX, bounds.minY, bounds.maxX, bounds.maxY)
+	}
+
+	/**
+	 * Run `query` over a small box around a point. Use for hit tests.
+	 *
+	 * @internal
+	 */
+	searchAtPoint(query: SpatialQuery, point: { x: number; y: number }, margin = 0): SpatialQuery {
+		this.spatialIndexComputed.get()
+		return query.searchPoint(point.x, point.y, margin)
+	}
+
+	/**
+	 * Structural audit of the index. Tests only.
+	 *
+	 * @internal
+	 */
+	validate(): void {
+		this.spatialIndexComputed.get()
+		this.index.validate()
 	}
 
 	/**
@@ -329,7 +383,7 @@ export class SpatialIndexManager {
 	 * @public
 	 */
 	dispose(): void {
-		this.rbush.dispose()
+		this.index.dispose()
 		this.lastPageId = null
 	}
 }
