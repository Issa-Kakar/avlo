# Commits listed here are ignored by `git blame` when
# `blame.ignoreRevsFile = .git-blame-ignore-revs` is set locally:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add the SHA of any pure-reformat / whitespace-only commit below.

# style: enable organizeImports and reformat (pure reorder)
c2c294614b9da17b65d57834d0a32a314743f388

# style(css): normalize to double quotes (pure reformat)
5eb3b67eaca4466f946d81e3e765e463ad536cc7
