We've made progress in this big picture refactor, and now its time to take a step back and look at everything from a birds eye view and be more explicit in the final goal architecture and clear up misconceptions so we can properly plan the next phases with the right end goal in mind. the big picture is that the Canvas.tsx React component is not the controller and is dictating the construction of everything and forcing it to be tied with React's lifecycle and prop drilling patterns, which is restricting the access of the methods and the lifecycle significantly, leading to messy excessive useRefs, creating/destroying tools leading to GC churn and messy code instead of a singleton created once, etc. the goal is to make everything dependency free to be able to construct as a singleton and to get things imperatively whenever needed, allow for the import of the roomdocmanager so the renderLoops/tools both are dependency free (the renderLoops still will depend on the canvas refs, but outside of that everything like snapshot, presence, gatestatus, etc. is all able to be imported cleanly in the manager), we can then unify the split MMB pan vs active tool pan logic all into pan tool since the pan tool construction doesn't need to depend on active tool anymore so we can properly unify pan tool to handle both since the function is the exact same. Currently, the tools are now all dependency free, so they are able to be constructed as a singleton. Now, i'll explain some things that i want in the end goal, because the docs aren't accurate or explicit about this and we need a source of truth, and a plan to match it. so: The end goal is that we have canvas.tsx call a useLayoutEffect that constructs CanvasRuntime.ts, a plain .ts file, which will construct the renderLoops, zoom, also perhaps the other module stubs could be condensed in it (if better), and, it'll import a tool registry module: the tools will construct themselves, so we'll create a tool registry and a helper to get the active instance, also perhaps a map for a lookup table, etc. this file will self construct the tool singletons. the canvasRuntime will import it, and we'll utlize the device-ui-store for getting the tool id, etc. and then for the event listeners: instead of canvas.tsx owning them, instead, when canvas.tsx calls canvasRuntime, canvas runtime will own an InputManager.ts file, where the only purpose of the input manager is to do the dom dirty work, attatching the event listeners to the canvas element from the module reference in the store and on the raw pointer events we'll forward them back to the "brain" the canvas runtime(i.e. onPointerdown..this.runtime.handlePointerDown(e)) and canvas runtime will convert coordinates, handle MMB pan check(prevent tools or panning from interrupting each other mid-gesture) and forward to the tool(tool.begin, .move pattern). so the input manager is dumb and handles the raw hardware and lets canvas runtime be the brain. the renderLoops need to be devoid of dependencies outside of the stagerefs(but we could inject ctx, but still either way we'll construct in the canvasRuntime not react). and we'll also need to make a getPreview, for the overlay renderLoop to know which preview to draw. Now, i'll be stating a few legacy things: the zero-mobile allowed checks i want to completely get rid of, we'll throttle mobile fps likely anyway even once we add mobile support but right now its in the way of iteration, we can add the mobile check where we register the module canvas if we really need it right now, we'll do whatever is pragmatic enough to not interfere with the refactor(whether its easier to remove or make a shim to satisfy renderLoop or add field to store etc.) Next: the room doc update activity is utterly useless UX, I plan to broadcast textID's in future the data shape is fine overall but don't worry about updating activity in canvasRuntime its not important. the suppress tool preview during MMB pan was for a legacy eraser cursor manually drawn in css in overlay that we needed to suppress, which is useless now. We need to completely get rid of the react imperative handles and eradicate them, the canvasStage "onResize" and also likely the removal of canvasHandle, we need to group and condense the tool logic pointer events(should be unified now), update the renderLoops to get rid of all unnecessary dependencies, unify the MMB/pantool and move all the logic for panning to the pan tool instead of making canvas.tsx handle it, etc. make everything cleaner, with seperation of concerns and responsibilites. also weigh if its worth it or if it makes this current refactor easier: we should just move the dpr/resize observer viewport update logic into Canvas.tsx since it'll become extremely thin after these changes anyway.