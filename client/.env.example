# ============================================
# AVLO CLIENT CONFIGURATION
# Phase 6 (Current) Requirements
# ============================================

# Required for WebSocket and API connections
VITE_WS_BASE=/ws
VITE_API_BASE=/api

# Optional - defaults to 14 if not set
# VITE_ROOM_TTL_DAYS=14

# ============================================
# DEBUG FLAGS (Development Only)
# ============================================
# VITE_DEBUG_RENDER_LAYERS=false